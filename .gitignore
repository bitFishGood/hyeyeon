git rm -r --cached client/node_modules
git rm -r --cached server/node_modules

git add .
git commit -m "Fix: ignore node_modules and prevent segfault"
git push origin main

node_modules/
**/node_modules/
